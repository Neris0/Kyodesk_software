<Window x:Class="Kyodesk.Views.NovoArtigoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Novo Artigo"
        Height="460" Width="720"
        WindowStartupLocation="CenterOwner"
        Background="#F5F6FA"
        ResizeMode="CanResize">

    <!-- Recursos locais simples -->
    <Window.Resources>
        <SolidColorBrush x:Key="BrandBlack" Color="#121212"/>
        <SolidColorBrush x:Key="CardBrush"  Color="White"/>
        <SolidColorBrush x:Key="CardBorder" Color="#E0E0E0"/>
        <Style x:Key="LabelCell" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Cabeçalho -->
        <Border Grid.Row="0" Background="{StaticResource BrandBlack}" CornerRadius="10" Padding="12" Margin="0,0,0,10">
            <TextBlock Text="Novo artigo" Foreground="White" FontSize="16" FontWeight="SemiBold"/>
        </Border>

        <!-- Card do formulário -->
        <Border Grid.Row="1"
                Background="{StaticResource CardBrush}"
                BorderBrush="{StaticResource CardBorder}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Título -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Título:" Style="{StaticResource LabelCell}"/>
                <TextBox   x:Name="TxtTitulo"
                           Grid.Row="0" Grid.Column="1"
                           Height="28" Margin="0,0,0,10"/>

                <!-- Categoria -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Categoria:" Style="{StaticResource LabelCell}"/>
                <ComboBox  x:Name="CmbCategoria"
                           Grid.Row="1" Grid.Column="1"
                           Height="28" Margin="0,0,0,10"
                           IsEditable="True">
                    <ComboBoxItem Content="Infraestrutura"/>
                    <ComboBoxItem Content="Sistemas"/>
                    <ComboBoxItem Content="Protheus"/>
                    <ComboBoxItem Content="Redes"/>
                </ComboBox>

                <!-- Conteúdo -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Conteúdo:" Style="{StaticResource LabelCell}" VerticalAlignment="Top" Margin="0,2,8,0"/>
                <Border Grid.Row="2" Grid.Column="1"
                        BorderBrush="{StaticResource CardBorder}"
                        BorderThickness="1"
                        Background="White"
                        CornerRadius="6">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <TextBox x:Name="TxtConteudo"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 BorderThickness="0"
                                 Padding="8"
                                 MinHeight="220"/>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Border>

        <!-- Botões -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancelar"
                    Width="110" Height="32" Margin="0,0,8,0"
                    Click="Cancelar_Click"/>
            <Button Content="Salvar"
                    Width="110" Height="32"
                    Click="Salvar_Click"/>
        </StackPanel>
    </Grid>
</Window>
